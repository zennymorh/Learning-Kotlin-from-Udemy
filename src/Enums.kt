fun main(){
//Enums are used to declare your own data types, a set of constant values
    var carDirections = Directions.North
    if (carDirections == Directions.South){
        //do something
    }
        //You can't assign a variable to something that is not in the scope
}